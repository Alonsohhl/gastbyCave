import React from "react";
import { SearchBoxTextStyle } from "./SearchBoxText.style"

const SearchBoxText = () => {
    return(
        <SearchBoxTextStyle>
            <span className="exax">Where?</span>
        </SearchBoxTextStyle>

    );
}

export default SearchBoxText;